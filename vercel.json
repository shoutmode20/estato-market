{
  "version": 2,
  "builds": [
    {
      "src": "server.js",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    { "handle": "filesystem" },
    { "src": "/api/(.*)", "dest": "/server.js" },
    { "src": "/property/(.*)", "dest": "/server.js" },
    { "src": "/sitemap.xml", "dest": "/server.js" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
